import { OrderTypePipe } from './order-type.pipe';

describe('OrderTypePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderTypePipe();
    expect(pipe).toBeTruthy();
  });
});
