import { DateLocalPipe } from './date-local.pipe';

describe('DateLocalPipe', () => {
  it('create an instance', () => {
    const pipe = new DateLocalPipe();
    expect(pipe).toBeTruthy();
  });
});
