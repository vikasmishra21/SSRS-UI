export enum OrderType {
  Diary = 1,
  PPM = 2,
  PR = 3,
}

export enum TableVersion {
  Staging = 1,
  Finalized = 2,
}

export enum Measure {
  Count = 1,
  Percentage = 2,
  Mean = 3,
  Sum = 4
}

export enum FileType {
  CSV = 1,
  SPSS = 2
}

