import { EleFocusDirective } from './ele-focus.directive';

describe('EleFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new EleFocusDirective();
    expect(directive).toBeTruthy();
  });
});
